#include<stdio.h>
#include<string.h>
int main()
{
    char s[1000],sb[100];
    int i,ls,lsb;
    gets(s);
    gets(sb);
    ls=strlen(s);
    lsb=strlen(sb);
    for(i=0;i<=(ls+lsb);i++)
    {
        s=s[i]+
    }
    puts(s);
    return 0;
}
